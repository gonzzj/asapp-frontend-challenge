import styles from './CitiesSelect.module.scss';

const CitiesSelect = () => {
  return (
    <div className={styles.select}>
      Get started by editing
    </div>
  );
};

export default CitiesSelect;
